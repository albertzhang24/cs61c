1. .data is the read-write section that contains global/static variables. 
   .text is the read-only section that contains code.
   .word is the directive to make 32-bit words. 

2. 34, which is the 9th nonzero fib number. 
3. 0x1000008
4. 233

1. t0 and t3 is k. 
2. t4 is source, and t6 is dest. 
3. Each element of source array is copied into dest array until a 0 in the source array is reached. 
4. Shifting t0 and storing that index value into t3, and then using pointers to set dest array values to source array values. 

