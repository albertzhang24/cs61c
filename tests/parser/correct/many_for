struct empty {};

struct empty *random (int x, int y) {
  for (x = 0;;) {
    break;
  }
  for (; x < y && x > y;) {
    Print (x);
  }
  for (;; ++x) {
    break;
  }
  for (x = 0; x < y;) {
    x = x + 1;
  }
  for (x = 0;; x ++) {
    break;
  }
  for (;x < y && y < x; y = x) {
    Print (y);
    continue;
  }
  for (x = 0; x < y; x = x + 1) {
    ;
  }
  return (struct empty *) NULL;
}
